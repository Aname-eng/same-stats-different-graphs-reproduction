# Same Stats Different Graphs：自适应选 N、全程固定点集与复杂校徽演化

你是本项目的本地代码执行代理。本轮任务是修改现有项目、实际运行测试并交付可审计结果，不是再给一份泛泛的算法建议。不要因为本任务较大而只回复计划；在当前执行环境中完成能完成的实现、测试和记录，明确未完成项。

## 0. 项目定位与工作边界

仓库：Aname-eng/same-stats-different-graphs-reproduction。
仓库内交付：本提示词位于根目录 gemini_fixed_n_shape_evolution_prompt.md，配套代码位于根目录 moment_preserving_moves.py。直接从远端拉取并读取这两个文件，不需要用户下载或转发聊天附件。
本提示词编写时实际核查的远端 main HEAD：66ec1e3a29c125137da4b446ea05e213465a8081。
该 SHA 只是检查点，不是要求回退到它。开始时检查本地路径、分支、工作区未提交改动、git remote 和最新远端 HEAD。不得 reset/clean/覆盖用户未提交内容，不得 force push。

先完整读取 README.md、README_复现记录.md，以及当前存在的交接文件和项目指令，再读以下实际代码：
- run_custom_target.py
- test_full_logo.py
- test_grouped_annealing.py
- image_to_points.py
- course_source/ 内统计计算与种子读取代码
- course_source/seed_datasets/Datasaurus_data.csv
- targets/南开校徽_components.csv
- 南开校徽.jpg

阅读相关失败记录和已有的 142/284/568 点生成流程。不要把文件名中的 perfect 或 README 的“成功”视为测试证据。保留已完成的 Circle/Flower 基准与旧输出，新实现、新试验写入独立目录。不要重写整份课程报告，也不要把任务扩展到校训；本轮优先完整南开校徽，架构需支持以后其他目标。

先确认目标：从一个足够密、可辨认且统计合格的恐龙点集，演化到外部八角星、两个圆环、内部“南開”均可辨认的校徽；几何保持原比例，不靠变形背景图或新增点作弊。

## 1. 不可违反的不变量

N 可以由每次实验的目标复杂度决定，不限于 142，也不要求是 142 的倍数。
但 N 的选择、恐龙加密和统计校准必须发生在正式第 0 帧之前。每一条正式轨迹必须满足：
- len(P_t) == N_star，所有点等权。
- 第 0 帧就存在全部 N_star 个点；此后禁止增删、复制、拆分、合并、重采样或隐藏/显示点。
- point_id 从第 0 帧到最后一帧保持不变且唯一；动态匹配改变的是目标归属，不是点的身份或数组行顺序。
- 不能把重合的复制点藏在第 0 帧，后面再散开制造“没有增加点”的假象。
- 所有接受状态与所有输出帧的五项统计量，保留两位小数后均为 [54.26, 47.83, 16.77, 26.94, -0.06]；样本标准差 ddof=1。
- 必须从真实坐标重算统计量，禁止修改显示数字、改用加权统计量或悄悄放宽阈值。
- 正圆不能被全局各向异性缩放成椭圆，文字与星形不得为匹配统计量而分别拉伸。

两次独立运行可以采用不同 N。若输出恐龙→花→校徽这样的连续链，则必须在整条链第 0 帧前联合确定一个 N，并验证该 N 对每个目标均可用；不能在花→校徽时扩点。

目标骨架的采样点、覆盖检查点、距离场像素不是实际数据点，数量可以不同，但必须在代码和输出中明确区分，不能混入点数、统计量或正式成品。

## 2. 第一优先级：修复统计验收口径

已核查的 run_custom_target.py 中 stats_allowed 使用 np.floor(stats * 10**decimals)，它不是四舍五入。例：54.261 与 54.269 在 floor(...*100) 下相同，但显示两位小数不同。

把“严格复现旧代码”的 legacy_floor 模式和本轮“显示两位小数严格相同”的 round2 模式分开。本轮 enhanced 模式必须统一使用同一统计签名函数进行接受检查、CSV 审计和图中文字生成；明确 halfway/tie 规则，不能混用 floor、Python round、NumPy round 和 Decimal。

先核查真实原始恐龙按选定口径的签名。若输入与要求不符，明确定位数据/读表/统计口径错误，不要把错误起点的签名当成合格基准。

优先保持初始完整精度均值 mu0 与样本协方差 Sigma0，并额外实施 round2 硬检查。对于新增 N 的恐龙，在第 0 帧前尽量校准到原始恐龙的完整精度 mu0、Sigma0；随后冻结这一参考，不与上一步滚动比较。

用 float64。记录距取整区间边界的余量和完整精度漂移；不能仅用 max_abs_difference < 0.01 验收。

添加正数、负相关系数、边界、NaN/Inf、ddof=0/1 混用的回归测试。独立复核已有可用 Circle/Flower 快照，并如实区分“旧 floor 口径通过”和“新 round2 口径通过”；不要未经重算就宣布旧结果全错或全对。

## 3. 在第 0 帧前自动确定本次 N

实现独立 preflight/planning 阶段：目标预处理→视觉预算→矩相容性→候选 N 试运行→冻结 N_star→正式演化。

N 不能仅根据图片分辨率、黑色像素数或主观“复杂度分数”确定。先固定输出画布、等比例数据坐标映射、marker 大小和允许的间隙/覆盖误差，再考虑：骨架总长度、细笔画数量、最小分离距离、端点、交点、转折、圆弧与星角的覆盖要求。

可以用各段 length/desired_spacing 加关键节点配额估计起始 N，但只能称为启发式估计，不是理论最小点数。选择合理的递增候选集合；142、284、568、1136 可以作为对照，但不要写死只支持这些数字。提供 --n auto / --n <integer> / --n-max 配置。

候选 N 的试验是独立运行，不属于正式动画。采用事先固定的质量门槛和预算，选“已测试候选中最小的达标 N”，不得写成已证明最小 N。不同 N 的预算至少按累计参与更新点数/N、目标函数评估次数和墙钟时间报告，不得只给所有 N 同样 100000 次单点扰动就声称公平。

未达标时区别：统计/几何相容性证据不足、点数覆盖不足、种子不合格、优化预算耗尽。更大 N 不能自动解决几何与矩的矛盾。达到 N_max 或预算上限后，如实报告，不能在正式演化途中补点。

## 4. 构造密集恐龙种子，不是简单复制

重用原恐龙的几何与局部密度信息，采用结构保真的分层重采样/局部加密，保留头、眼、身体、腿、尾等可辨认结构。不能假设 CSV 行顺序是轮廓顺序并直接连线插值，不要把独立肢体或眼睛连接起来。

禁止直接 np.repeat/np.tile 后当成完成：样本方差有 N-1 分母。若原来 n 个点各重复 k 次，新样本协方差变成 k(n-1)/(kn-1) 倍，通常不再满足原来的完整精度矩。

必要的统计校准只能在第 0 帧前完成。可以测试小幅白化-着色校准，但必须记录变换、位移和几何偏离；校准明显扭曲恐龙时应重采样或用约束优化改善种子，不得默默接受。这个初始化步骤不能被误用为演化过程中的目标全局拉伸。

验收：数量、唯一 ID、有限坐标、统计签名、重复点/最近邻距离分布、恐龙几何覆盖、无新增伪连接。保存 seed_N.csv、纯散点第 0 帧、seed_diagnostics.json，并标注这是“扩展恐龙种子”，不是原始 142 个观测。

## 5. 完整目标与矩相容的采样密度

将真实校徽拆为外圆环、内圆环、八角星、“南”、“開”，必要时再细分笔画/圆弧/边。
现有分组脚本的固定半径分区不能直接视为正确分层；应检查是否漏掉某个圆环、切断笔画或把纹理分错组。提取中心线/骨架时保留关键结构，生成诊断图并检验，不准以删除内字换取成功。

允许平移和统一比例缩放；必须保留 x/y 等比例，不能分别缩放图层。不要仅根据目标模板均值居中就假设采样均值已经相容。

设候选骨架位置 g_j、权重 w_j、原始完整精度 mu0、Sigma0。对每个候选 N，检查：
  sum(w)=1, w>=0
  sum(w_j*(g_j-mu0))=0
  sum(w_j*(g_j-mu0)*(g_j-mu0)^T)=((N-1)/N)*Sigma0
并加入关键笔画/圆弧最低质量约束。
这些权重只是规划密度，最终 N 个数据点仍等权。可先用线性规划检查，再用偏离参考覆盖密度的二次/KL 目标避免极度稀疏权重。

固定网格上的分数权重可行，不等于 N 个等权点已经可行；还要整数配额与沿骨架滑动修正，并复核误差。单个离散网格、单个缩放或某个完整精度矩检查失败，也不能证明整个 round2 区间无解。标明证据范围。

几何 1:1 不要求点密度均匀；上下可以更密。但所有字形必须具有实际覆盖，不得把内字权重压成零。保存各层配额与实际统计残差。

## 6. 全局目标：容量匹配为主，分层覆盖为辅

不要只用点到最近墨水的单向距离，也不要仅增加强排斥场。

构造总容量为 N_star 的目标 slots；每个 slot 是有范围限制的一小段笔画、圆弧或星形边，可让一个点沿段滑动调节矩。每个 slot 容量为 1，每个实际点负责一个 slot。

cost_ij = dist(point_i, slot_j)^2；通过全局指派或等价的有容量最优传输匹配。不能把 N×M 的矩形 linear_sum_assignment 误认为会覆盖 M>N 的每个目标。大规模近似必须说明质量守恒与覆盖如何保证。

目标示例：
  E = E_capacity_match + lambda_cov*E_layer_coverage + lambda_sep*E_local_overlap
覆盖项按图层/笔画分别归一化，使用明确的可接受距离容差；关键小笔画不能被长圆环的像素数淹没。排斥只作为弱辅助，距离尺度适应计划密度，禁止强行全局等间距。

动态更新目标归属，不要把原来离文字最近的恐龙点永久锁定在文字组。任何配额调整必须保留总 N、点 ID、最小结构覆盖和矩相容性，并记录调整。

先用小规模的精确指派作为正确性参照。N 较大时可分块固定 assignment 做若干提案，再全局重匹配；必须在同一个 assignment、目标与权重下比较 trial/current 能量，切换后重算当前能量。不得把缓存代理损失冒充每步精确最优传输损失。

## 7. 保矩更新核与针对停滞的提案

主提案用三点协同更新，而不是绝大多数被统计门槛拒绝的独立单点扰动。三点 ID 从全体点中选，组合要不断重叠，允许跨文字/外框协同，不能固定三人组或只在图层内部更新。

对所选三行 B (shape 3×2)：
  center = mean(B, axis=0)
  Z = B-center
  K = [[0,-1,1],[1,0,-1],[-1,1,0]]/sqrt(3)
  B_new = center + cos(theta)*Z + sin(theta)*(K @ Z)
其余行不变。
这保持 sum(x),sum(y),sum(x²),sum(y²),sum(xy)，因而保持均值、样本标准差和相关系数；浮点下仍必须硬检查。K 在点索引维度作用，不是在二维坐标平面旋转校徽。

先写单步、逆变换、长链、跨多种 N 的测试。可以参考仓库根目录的 moment_preserving_moves.py，但它只是更新核，不是已验证的校徽优化器。

为避免只有随机微扰，提供可审计的提案混合：
A. 对称随机角度三点提案，覆盖不同空间距离尺度。
B. 匹配引导的三点候选。在固定三个目标坐标 Y（slot 可取当前最近可行点）且只看平方匹配损失时：
   a=<Z,Y>, b=<K@Z,Y>, theta_star=atan2(b,a)。
   这是这条三点运动轨道上固定目标平方损失的最优角候选，不是整个问题的全局最优。a,b 同时近零时应回退。用实际包含覆盖项的总损失验收，必要时限制步幅/回溯。
C. 明确停滞后，增加跨区域提案与较大角度；必要时用若干保矩三点变换的组合形成更大块提案。不要先引入复杂流形库。

越界、非法数值或统计失败应拒绝，不能 clip、逐点 snap 到骨架或坐标取整修复。额外样本边界要配置并与渲染视窗区分；检查现有 0<x,y<100 是否真是任务约束，不能悄悄放宽。

随机对称提案可用 exp(-deltaE/T) 接受。匹配引导、状态依赖选点、回溯等可能破坏提案对称性；未实现 Hastings 校正时，明确称为约束启发式退火，不声称具备严格采样或全局收敛保证。

## 8. 防卡死调度与轨迹真实性

所有图层从初始目标函数中就存在，不采用“先做纯空心星形并永久冻结，再补字”的流程。
整体迁移→分层覆盖→局部精修：渐进收紧 slot 与覆盖容差，调节温度和角度，保留跨区域补偿能力。温度依据实际 deltaE 尺度标定，不能照搬旧脚本的无量纲 accept_bad 概率并冒称标准 Metropolis 温度。

实现停滞检测：监控固定定义的验证损失、各层覆盖率、最差笔画误差、有效接受率、实际位移及统计拒绝率。区分“满足目标而稳定”和“覆盖未达标但能量停滞”。预先设置停滞窗口与改善阈值，记录触发原因和恢复效果。

依次尝试全局重匹配、调整提案空间跨度/角度、reheat；仍失败可另起独立随机种子试验。至少采用 3 个独立优化随机种子，计算总体成功率、最差层误差和耗时分布，不仅展示最好一张图。

重启是独立轨迹。不能将不同重启的中间状态拼接成一条动画；同一轨迹也不能无说明地跳回 best state。最终可选择一条完整通过验收的轨迹展示，同时保留所有失败试验记录。

特别注意动画：两个合法状态之间做线性坐标插值，通常不保持协方差。需要过渡子帧时，用同一三点变换的 theta(t) 或合法保矩路径生成并逐帧验证，不能 crossfade 两套散点、线性 tween 或用目标背景补成品。正式成品只画实际数据点，诊断叠图另外保存。

## 9. 对照、独立验收与交付

先用 Circle/Flower 回归，再做完整校徽。同一 N、同一起点、可比预算比较：
A 旧单向距离+原更新规则；B 容量匹配+原更新；C 容量匹配+保矩更新；D C 加停滞恢复。
原规则的 floor 与本轮 round2 要明确标注，可额外提供统一 round2 口径的基线，不能混口径比较“统计合格率”。若预算不足，明确哪些对照未运行，不伪造表格。

独立验收程序不能只信优化日志。重新读取坐标及点 ID，检查第 0 帧和全部输出帧、以及实际接受更新的审计记录。大规模轨迹可用初始状态+每次三点更新日志无损重放，避免保存每步完整数组；记录高精度角度、ID、随机种子及代码版本。

必须记录：N、ID 集合与顺序、round2 签名、完整精度漂移、各图层/关键笔画覆盖率、point→target 与 target→point 误差、最差层误差/高分位距离、重复/异常聚集、窗口边界、温度、匹配刷新、提案接受率、停滞恢复事件、实际运行时间与代码 SHA。

量化达标不自动等于文字可辨认；必须查看不叠目标背景的终帧与原素材对照。没有可靠视觉检查时，将视觉可辨认性标为待验，不准写“完美校徽”。不得要求当前状态能量每步单调下降；退火可以接受坏步，best-so-far 和 current 必须分开记录。

建议产物（名称可按现有目录调整）：
- 可直接运行的脚本、依赖与命令；--n auto 和手动 N 均可用。
- planning.json：候选 N、统一质量门槛、试验预算、选定理由、失败原因。
- seed_N.csv、target_layers/slots、配额和矩相容性报告。
- run_manifest.json、更新审计日志、metrics.csv、终态及中间帧坐标。
- 纯散点起点/终点/动画、独立诊断叠图与验证报告。
- 测试及简短实现说明，明确原论文复现部分与新增方法部分。

在独立功能分支开发，不直接覆盖 main。保存代码、轻量可复现实验产物与失败证据；检查 git remote 的目标仓库，按现有权限正常提交推送，无权限则保留本地提交并准确报告。不得提交密钥、虚拟环境、无关文件或海量重复图片。

最后汇报实际修改文件、执行命令、所选 N、测试/可视化证据、各随机种子成败、仍未解决的问题、commit SHA 与 push 状态。把“已实现”“已运行”“已验收”分别写清楚。
