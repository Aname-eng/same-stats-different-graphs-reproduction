# 当前任务：修复目标矩相容、真实自动选 N 与校徽验收闭环（V2）

TASK_ID: SSDG_REPAIR_V2_20260915
TASK_STATUS: READY_FOR_EXECUTION，尚未实现或验收本轮修正
REPOSITORY: Aname-eng/same-stats-different-graphs-reproduction
WORK_BRANCH: feature/gemini-fixed-n-evolution
REVIEWED_BASE_COMMIT: 3042ad37ad02ea035e30e5049b2826e9fcca5602

你是本项目的本地代码执行代理。请实际修改代码、运行实验、检查图像与动画、执行失败测试并提交结果，不是仅写计划或复述算法。本文件是本轮任务单；与上一轮 gemini_fixed_n_shape_evolution_prompt.md 的阶段安排、完成表述冲突时，以本轮修正为准，上一轮不变量继续有效。不要通过修改本文件的要求把失败变成通过。

## 0. 为什么做：修的是目标和验收，不是再盲目增加迭代

用户要的是：根据目标细节在第 0 帧之前选择足够的点数 N，以真正密集且可辨认的恐龙为起点，让同一组等权点演化成校徽；所有接受状态和正式输出帧统计合法；校徽保留八角星、两条真实圆环、内部“南”和“開”，几何比例不变。

上一轮保矩三点更新、固定 N、固定 point_id 和容量匹配有实际进展，必须保留。尚不能验收完成的主要原因是：目标点集本身矩不相容；自动选 N 尚未检验真实演化结果；验收只把不变量当作完成；缺少连续动画；部分对照实验的名称与执行逻辑不一致。

本轮成功不以“接受率很高”“MSE 降低”“15 个快照统计通过”定义。必须分别证明全过程不变量、末帧几何、规划有效性和交付完整性。

不要扩展到校训、新图案、GPU 平台或重写整份课程报告。算法运行不得逐步调用语言模型。优先修复最小必要链路，保留已经有效的代码。

## 1. 启动与分支安全

1. 检查工作目录、git status、当前分支、remote 和远端最新 HEAD。先 fetch，再安全同步上述功能分支。3042ad3 是历史审查基线，不是要求回退；不得 reset --hard、git clean、force push，或覆盖用户未提交改动。
2. 在现有功能分支继续，不要回到 main 从零重写；如当前工作区有冲突，保留它并使用独立 worktree 或其他不破坏已有内容的方法。不得自行合并 main。
3. 完整阅读本文件、gemini_fixed_n_shape_evolution_prompt.md、moment_preserving_moves.py、README 和现存项目交接指令。
4. 阅读实际代码：src/stats_audit.py、src/dino_densifier.py、src/target_emblem.py、src/planner.py、src/nankai_optimizer.py、src/independent_verifier.py、src/compare_methods.py。
5. 阅读原始 Datasaurus CSV、南开校徽.jpg、目标 CSV/诊断、planning.json、已有实验 metrics 和审计报告。自行查看真实原图、初始散点与末帧纯散点，不得仅凭日志声称看过图像。
6. 为每项审查发现记录“当前仍存在 / 已修复且有证据 / 与审查不同及证据”，不要机械重复修复已有改动。现有实验作为 V1 留档，本轮输出使用独立的 V2 目录，不能覆盖历史失败证据。

## 2. 历史审查发现：请用实际文件复核，不把这里的约数写死

以下依据 3042ad3 的代码与保存结果。它们是修复线索，不是新版本运行结果。

### 2.1 固定目标与统计量不相容

文件 targets/target_slots_568_diagnostics.json 记录的实际 Slots 五项统计约为：
[55.39045103, 50.83105419, 17.26853063, 25.85182856, -0.06722718]。
当时冻结参考约为：
[54.26327324, 47.83225282, 16.76514204, 26.93540349, -0.06447185]。

src/target_emblem.py 使用固定配额比例和极角权重随机采样，没有把矩约束落实到最终 N 个等权目标位置。几何中心对齐不等于样本均值对齐。不能把独立探索脚本里的 LP success 当作最终 Slots 已相容。

对于当前固定 N 点 Q、固定精确均值/协方差的 P，以及未加权平方欧氏匹配 MSE：
E(P,Q) = min_pi mean_i ||p_i - q_pi(i)||^2。
令 C_P、C_Q 为分母 N 的经验协方差，则有下界：
LB_mean = ||mu_P - mu_Q||^2；
LB_cov = tr(C_P + C_Q - 2 * sqrt(C_P^(1/2) C_Q C_P^(1/2)))；
LB_total = LB_mean + LB_cov。
样本协方差 Sigma（ddof=1）要先乘 (N-1)/N 得到 C。

按上述历史数值重算：LB_mean 约 10.263339，LB_cov 约 1.426578，LB_total 约 11.689917；已保存的对照 Method D MSE 约 11.692。因此，它已经接近当前错误固定目标的误差下界，不能把剩余结构失真一概归因于局部最优或退火次数不足。

务必自己重算。这个下界针对固定点目标、特定精确参考矩和指定损失；不是整个两位小数容许区间、连续区段目标或复合损失的统一下界。非零下界不证明真实校徽无解；下界为零也不证明结构覆盖通过。只报告协方差项而漏掉均值项，是错误的。

### 2.2 自动选 N 没有闭环

src/planner.py 把 937.0 和 250.0 写成估计长度，用 nominal_spacing <= 2.0、text_points >= 80 和恐龙合法性决定通过，没有候选点数的真实优化质量验收。
slot_diag 的异常/相容性未进入 passed_all；所有候选失败时仍 fallback 到 568，随后声称它是最小达标候选。正式入口默认文件名也固定为 568。

这些最多是启发式预算，不能代表按目标实际效果选出的最小达标候选。

### 2.3 验收名称与验收内容不一致

src/independent_verifier.py 的 is_fully_accepted 仅依赖 count、IDs、统计签名和边界，几何距离未参与判定，恐龙第 0 帧也因此被标记 fully accepted。
第 0 帧不变量通过是正确的；把它解释为校徽完成是错误的。
此外，覆盖只检查随机 Slots，无法发现根本没有抽入 Slots 的缺失笔画。

### 2.4 对照与动画

src/compare_methods.py 中 Method D 名称包含停滞恢复，实际没有相应触发/恢复逻辑；C/D 还同时改变角度提案，不能把接受率差异归因于恢复机制。
Method A 实际沿 floor 规则更新，round2 只是对该轨迹候选的另行计数，不是独立 round2 基线。
上一轮每个主运行只保存 0、15k、30k、45k、60k 五个状态，没有交付连续演化动画。动画不是可选的高帧率美化项目。
报告里“边界安全余量 >2.5”也不符合所存坐标；用实际极值重算，不能复用文字断言。

## 3. 不变量：每一项都是硬约束

- N 可以随不同独立实验变化，不限 142，也不必为 142 的倍数。正式一条轨迹从 frame 0 起，N、等权性质、唯一 point_id 和行顺序全部固定。
- 禁止途中增删、复制、分裂、合并、重采样、重编号或隐藏点。禁止把重合复制点藏在起始画面，后面散开制造没有分裂的假象。
- 两位小数签名固定为 ('54.26','47.83','16.77','26.94','-0.06')，标准差 ddof=1。统计、接受检查、独立审计和图中文字必须使用一致且有测试的舍入口径，不能混用 floor 或仅检查差值 <0.01。
- 从真实原始 CSV 计算完整精度 mu0 和 Sigma0，不把人工抄写的有限小数常量称为完整精度。扩展恐龙只允许在第 0 帧前校准并检查几何；正式演化冻结实际合法起点的参考矩，优先保持其完整精度，并额外执行 round2 检查。
- 全部点必须有限且满足事先定义的坐标边界。实际余量要重算；禁止通过裁剪越界坐标修复，因为裁剪会破坏矩。
- 三点协同作为原子步骤是允许的，不必强迫单点更新。所有接受状态和所有显示子帧都必须统计合法；目标匹配改变目标归属，不改变点身份。
- 禁止演化中白化/着色整个点集、事后仿射拉伸目标、对不同图层分别变形以满足统计量。允许在规划阶段对完整几何作统一比例缩放和平移，并冻结相应坐标映射。
- 成品必须是实际数据的纯散点。诊断底图另存，不得把目标图叠在成品里弥补缺笔画。目标检查点、像素和 Slots 都不是数据点。

## 4. P0：先建立会识别失败的验收器

先把不变量、末帧几何和任务完成拆开，禁止继续复用含糊的 fully_accepted。
建议显式字段：
- invariants_pass：本状态的 N、IDs、有限性、边界和统计签名。
- trajectory_invariants_pass：所有接受状态重放检查通过、所有输出帧检查通过。
- final_geometry_pass：只对终态使用完整结构真值和预先冻结阈值检查。
- planning_pass：真实候选试验流程及选择规则通过。
- deliverables_complete：必需文件存在、可打开、可复核，不是只有路径字符串。
- task_pass：上述必要条件的合取；有未完成项时 false，并列 reason_codes。

不要要求初始恐龙或所有中间帧长得像校徽。任务完成看全过程不变量与末帧几何的组合，不看所有帧都达到终态。

### 4.1 独立几何真值

从原始校徽提取并检查完整几何 G_verify：两条真实圆环、八角星的边与角、南/開关键笔画。不能简单把 mid_ring 名称当成某条真实圆环：旧分区可能混有英文校名或年份，必须核对实际原图、圆环半径和笔画归属。其他英文/年份装饰是否纳入要在目标清单说明，本轮不自动增加其逐字可读要求，但不得以其取代必要圆环。

保存每层/关键笔画的真值、长度、端点/交点/转折及坐标变换。G_verify 不得从随机抽出的 N 个 Slots 反推，也不得随当前结果删去难覆盖部位。可以与训练损失使用同一真实几何，但验收采用独立的足够密、确定性取样，并记录间距误差。

先固定 output_canvas、数据轴范围、equal aspect、marker 大小和 gap/coverage 容差，把参数写进配置并散列。阈值须有视觉尺度依据，不得看过失败结果后不断放宽直至通过；如有合理修改，保留旧配置和旧结论，按新配置重新评估全部对照。

对各层和关键笔画分别报告：长度加权 coverage = covered_length/total_length、点到真实形状距离、真值到点距离的 mean/p95/max、最大连续未覆盖弧长/笔画长度、端点与转折覆盖。训练时上下较密，不意味着验收时可以降低左右圆弧权重。

几何门槛必须覆盖上述必要结构，不能只看全局平均值；细笔画、左右圆弧都不能被长外圈或上下极点的密度淹没。最终给出纯散点视觉检查，明确哪些字符可辨、哪些仍有断裂，不得把数值通过自动写成已目视辨认。

### 4.2 先做负例测试

至少增加以下可重复测试：
1. 合法恐龙 frame 0 可以 invariants_pass，但不能仅凭此得到 final_geometry_pass/task_pass。
2. 对一个有明确笔画真值的受控样例，移走关键笔画附近的点，并保持 N/ID 不变：几何子验收必须失败，即使该笔画不在随机 Slots 中。测试几何子验收本身，不能靠统计失败替代它。
3. 压扁圆环或丢失一条圆环：几何子验收失败。
4. N 改变、重复/缺失/乱序 point_id、NaN/Inf、越界、统计跨舍入边界：不变量失败。
5. 无快照、缺 frame 0、缺真正末帧、缺动画、缺配置/输入证据：不能因空列表 all() 为 true 而通过。
6. 所有候选 N 失败，或目标生成抛出异常：planner 必须明确失败，不得返回默认 568 或 task_pass=true。

这一步应先显示旧验收为何误判，再显示修复后测试通过，保存命令与实际结果。

## 5. P0：让最终目标密度与矩相容，不再只做极角参数碰运气

保留原比例的完整几何。若目标位置需要平移，则整个几何、Slots、G_verify 和图层一同平移并冻结，不能只移动一部分或只改评分坐标。

在真实几何候选位置 g_j 上规划非负权重 w_j：
sum(w)=1；sum(w_j*(g_j-mu0))=0；
sum(w_j*(g_j-mu0)*(g_j-mu0)^T)=((N-1)/N)*Sigma0。
加入关键笔画/圆弧最低质量和合理的密度限制，防止把文字或左右轮廓的权重压为零。

可先线性规划检查当前离散模型，再以相对合理覆盖密度的二次偏差或 KL 项选择不塌缩的权重。注意 c=ones 且 sum(w)=1 时，LP 目标恒定，不能把它称为权重均匀化或方差最小化。

分数权重可行不是最终 N 个等权点可行。必须进一步做整数配额及沿原笔画/圆弧的位置调整，重算最终 N 个目标位置的均值、协方差、完整几何覆盖和密度。一次随机按权重抽样会重新产生矩误差，不能抽完便跳过验证。

实现可以选以下较简单且能达标的路线，不为复杂而复杂：
A. 容量总数为 N 的有限笔画/圆弧区段 Slots，每个容量为 1，允许局部沿段滑动；配额和区段范围避免所有点挤进少数文字区域。
B. 先通过沿几何的受约束位置优化构造 N 个等权、实际矩相容且覆盖达标的固定目标 Q，再用已有效的容量匹配保矩演化。B 若达到同样验收，不强迫额外实现 A。

两条路线都不得用对 Q 的各向异性白化/着色冒充保持几何；不得通过删文字、重复重合 Slots 或添加自由离群点来凑矩。有限的笔画容差应提前配置并计入几何误差，不是事后自由逃逸。

保存目标诊断：原始/实际参考矩、最终均值差、协方差残差、round2 签名、各层整数点数、G_verify 覆盖、变换参数、密度分布，以及适用时的 LB_mean/LB_cov/LB_total。LP/QP 状态、数值容差和等权修正结果分别记录。

实现下界重算回归测试：Q=P 时约为 0；纯平移的均值项等于平移向量平方范数；协方差不匹配不能漏报；实际匹配 MSE 不应小于适用下界（允许有记录的数值误差）。使用对称矩阵稳定计算，不能把显著负特征值悄悄截断当作正常。

不能把下界降低称为相容保证。固定网格/单个缩放无可行解只说明该模型失败；允许在规划阶段尝试记录过的统一缩放、密度和候选 N，不得把预算耗尽报告为已证明全局无解。

## 6. P1：接入保矩优化并对完整目标评分

保留三点更新、解析角度候选、跨空间抽点和动态容量匹配，不要回到单向最近墨水距离。
目标至少综合容量分配与分层真实覆盖；必要时增加弱的局部重合抑制或离形状惩罚。密度本来可以不均匀，不能施加强制全局等距排斥。

解析 theta* 只最小化选定三个点、固定目标位置下的平方匹配子问题。加入覆盖、重合或可滑动 Slots 后，它只是候选，最终接受必须按本次真正采用的完整损失判断，不能仅更新旧的三点 matching delta 后宣称复合能量下降。

assignment、Slots、几何变换、权重或温度阶段更新时，重新计算当前能量；新旧候选必须在相同配置下比较。缓存可加速，但先通过小样本全量重算测试；不能把分块固定 assignment 的代理损失声称为每步精确最优运输。

停滞恢复要有真正执行的提案/温度变化及触发前后指标，不能只写 action 字符串。保留当前状态轨迹与 best_state 的区别；不能在动画末尾直接跳到历史 best_state。可选择历史最佳时点的真实轨迹前缀作为展示，但要注明。

对 errs.sum()==0、无合法三元组或候选越界等情况给出明确处理。计划快照和检查点保存不能因为越界提案处的 continue 被意外跳过。尽可能用事件重放保证审计完整性，而不是仅在每 15000 步检查一次。

先完成一组端到端的目标、优化、几何验收小试，再扩大多随机种子；不要花费预算批量优化一个已经被目标诊断否决的模板。

## 7. P1：真实自动选 N，并真正驱动正式运行

实现统一入口或已有入口的扩展，支持 --n auto、--n <integer>、--n-max、候选 N 配置、预检预算、随机种子、目标路径、输出目录和验收配置。

启发式骨架长度/间隙预算只用于提出起始候选，不是完成判定。长度来自实际几何及其坐标变换，不能使用 937/250 一类未说明来源的固定常量代替不同目标。

对每个实际测试候选，执行：生成并验收密集恐龙 → 目标矩相容/覆盖准备 → 真实小规模优化 → 末帧几何验收。保存运行目录、seed、命令、原因和失败类型，不得只生成 Slots 就称为候选试运行完成。

在预先固定门槛及预算下，选已测试候选中最小的达标 N。必须把结果接入种子文件、目标文件、优化器、审计和动画，不能 planner 选一个 N，正式入口仍读 seed_568.csv。

没有候选通过时：chosen_n_star=null，status=NO_QUALIFIED_CANDIDATE，包含错误证据并返回非零退出码。不静默 fallback，不改变正式轨迹 N。

区分 TARGET_PREPARATION_FAILED、SEED_INVALID、COVERAGE_INSUFFICIENT、OPTIMIZATION_BUDGET_EXHAUSTED、GEOMETRY_FAILED 等，不能把所有失败统一叫点数不足。更大 N 并不自动消除几何与矩矛盾。

预算至少报告实际提案数、累计参与更新点数/N、全局匹配次数、完整目标评估次数和墙钟时间。候选评估参数和成功规则先冻结；正式选定 N 后，至少对 3 个优化随机种子运行验证并逐一报告几何通过/失败。不能只保留最好一次并声称全部成功。

密集恐龙生成也要检查重复点、最近邻距离分布和头/眼/身体/腿/尾结构。只根据 kNN 插值不能自动宣称解剖拓扑已验证；实际查看并给出局部几何证据。新增 N 的所有点在正式第 0 帧前存在，初始化校准不能搬进演化阶段。

## 8. P1：输出真实连续动画与可重放轨迹

交付 GIF 或 MP4 中至少一种可直接播放的纯散点动画，不是五张稀疏快照的轮播。无需强迫 60 fps 或单点更新；帧率、播放时长与采样密度由配置明确，必须能看清恐龙向校徽的连续变化。

记录实际接受的三点事件：事件序号、对应固定 IDs/索引、theta、必要的配置变化及初始状态，使另一进程可从 seed 重放。记录全局匹配变化不等于改变点 ID。保存必要检查点，抽查重放与保存状态相符。

需要平滑子帧时，对真实三点事件使用：
B(t)=center+cos(t*theta)*Z+sin(t*theta)*KZ，t 从 0 到 1。
它只改变该事件的三个原点，保持点数、身份和矩。每个显示子帧仍须检查舍入及边界；路径端点合法不能替代路径合法。必要时限制/分解提案或拒绝路径越界的事件，不得裁剪坐标。

不要在线性插值两个完整关键帧后只显示合法端点的统计数字。不要重新生成无关的校徽点集后淡入，也不要用图像补帧伪造受约束坐标轨迹。

对所有接受状态进行事件重放统计/身份审计，对所有正式显示帧从坐标重算并保存签名及来源索引。只审查稀疏快照时必须叫 snapshot_audit，不得称全生命周期独立审计。

输出：纯散点 frame0、若干中间帧、最终帧、动画及对应帧坐标/索引映射。保持相同坐标轴、比例和 marker 配置，诊断底图不得进入成品。实际打开动画检查，不得只因文件存在就报可播放且连续。

## 9. P2：修正实验对照与结论，不强求复杂方法胜出

在主要链路可验收后再做这一步，不用大量对照取代校徽本身完成。

A：单向距离 + 单点扰动，实际沿 round2 合法轨迹运行；legacy_floor 另列为历史模式。
B：容量匹配 + 单点扰动，round2。
C：容量匹配 + 保矩三点，无恢复。
D：与 C 相同的核心目标/提案配置，仅开启真正实现的恢复模块；如另研究残差抽点，独立开关或明确这是复合方法，不能归因于单一机制。

尽量复用实际优化引擎和配置，不再复制一份功能不一致的简化 D。记录实际启用的功能、随机种子、触发次数及事件。零触发必须写零，不能声称已证明恢复有效。

所有方法使用同一 N、同一起点、同一目标真值和相同统计口径；报告等归一化更新预算与时间，不把单点/三点相同原始步数称为所有意义上的公平。共享末帧几何评分与适用匹配指标，接受率只作诊断，不作成功标准。

允许 C 与 D 无显著差异，也允许简单方案更好。以实际数据记录，不挑选结果维护上轮叙述。修正误称数学保证、全部规范完成、安全余量和距离方向等表述，保留原始报告作为带版本的历史记录，不抹去证据。

## 10. 交付、证据与完成状态

以仓库内文件为唯一交接，不要求用户下载聊天附件或补发当前仓库已有材料。建议在 output/repair_v2/<run_id>/ 与 analysis/repair_v2/ 保存；可以适应当前工程，但实际路径必须写入 manifest。

必须交付并验证：
1. 修复代码与正/负回归测试；实际运行命令及环境依赖版本。
2. 验收配置、完整几何真值与分层诊断、目标矩残差与适用下界重算、最终目标/区段定义。
3. 真实候选 N 试验表、选择理由、失败原因、最终冻结的 N 和密集恐龙诊断。
4. 至少三组优化种子逐组结果，原始数据、匹配/覆盖指标、事件重放审计、末帧几何通过状态。
5. 可播放纯散点动画、关键帧和源坐标映射；明确哪些轨迹导出了动画，不必为凑数量重复三份同样展示。
6. completion_report.json 与简短 Markdown，明确 IMPLEMENTED / EXECUTED / VERIFIED / FAILED_OR_PENDING，逐项给出证据路径；不得只有一个全部通过布尔值。
7. manifest 记录 input 文件哈希、目标/阈值配置哈希、实际运行代码版本、环境、随机种子和实际产物哈希。运行时有未提交代码则记录 dirty 状态与补丁/源码哈希，不能只写父提交 SHA 冒充完全可复现；条件允许先提交代码再运行，然后单独提交结果。

将代码、任务执行状态和必要轻量结果提交推送到当前功能分支，不自行合并 main，不提交缓存/秘密/不必要的大文件。报告实际 code commit 和 results commit（若有区别），区分本地提交成功与远端推送成功。

若全部必要项达标：task_pass=true，并列出配置范围和已测试 N/种子，不能声称全局最优或对任意复杂图形保证收敛。
若仍有不通过项：task_pass=false，保留实际进展、失败证据及下一条具体工作。不得把必需动画、真实自动选 N 或关键字形覆盖改称可选未来工作后宣布全部完成。

最终对用户只需说明：这一轮实际修了什么；选择哪个 N 及证据；全过程统计/点身份是否通过；两圆环、星形、南/開分别是否通过几何验收；动画在哪里；还有什么失败；远端分支和提交 SHA。没有看过图，就不要写视觉清晰；只有下界推导，就不要写校徽已收敛。

执行顺序：失败可检验的验收 → 目标矩相容及真值准备 → 单组端到端验证 → 自动选 N/多种子 → 连续动画与重放审计 → 对照与报告收口。先保护原有工作，再直接开始实现。
