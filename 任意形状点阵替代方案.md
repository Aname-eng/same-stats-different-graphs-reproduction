# Draw My Data 的离线替代方案

原课程链接 `http://robertgrantstats.co.uk/drawmydata.html` 当前已不可访问。可以不依赖网站，使用 Windows 画图和 `image_to_points.py` 生成相同用途的 `x,y` 点阵数据。

## 画图

1. 打开 Windows **画图**，新建一张白底画布（建议 512×512）。
2. 用黑色、较粗的画笔画出轮廓，例如字母、猫、五角星或人物轮廓。
3. 保存为 PNG，例如 `my_shape.png`。不要使用透明底或深色背景。

## 生成 CSV 点阵

在项目目录运行：

```powershell
python image_to_points.py .\my_shape.png --output .\course_source\seed_datasets\mydata.csv --points 142
```

脚本会从黑色笔迹中随机但可复现地抽取 142 个像素点，缩放到课程脚本所使用的 0–100 坐标范围。输出文件有两列：`x,y`。

如果是“深色背景上的白色校训文字”，加上 `--light-ink`，让脚本抽取亮色文字而不是深色背景：

```powershell
python image_to_points.py .\校训.jpg --light-ink --output .\course_source\seed_datasets\mydata.csv --points 142
```

## 用课程代码继续扰动

课程源码支持把自定义形状作为起点：

```powershell
Set-Location .\run_default_dino_to_circle
Copy-Item ..\course_source\seed_datasets\mydata.csv .\seed_datasets\mydata.csv
python same_stats.py run mydata circle 20000 2 30
```

这会将手绘形状在保持统计量近似不变的约束下，扰动为内置圆形。该课程原始代码的目标形状仍限于 `circle`、`bullseye`、`dots`、线条、星形等内置形状；它并不能直接把“一个手绘图形”变成“另一个手绘图形”。

## 可复现性

默认随机种子为 `20260914`，相同 PNG 与相同参数将始终生成完全一致的 CSV。若要生成另一组抽样点，只需加参数，例如：

```powershell
python image_to_points.py .\my_shape.png --output .\course_source\seed_datasets\mydata.csv --points 142 --seed 7
```
